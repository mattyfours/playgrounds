<div class="p-4 w-full h-dvh justify-center flex flex-col items-center text-white">
  <h1 class="text-2xl">{{ title }}</h1>
  <h2 class="text-sm">{{ subtitle }} efq</h2>

  <ul class="mt-4">
    {% for item in list %}
      <li>{{ item }}</li>
    {% endfor %}
  </ul>
</div>
